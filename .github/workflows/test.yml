name: Test

on:
  workflow_dispatch:
  workflow_run:
    workflows: [Deploy]
    types: completed

jobs:
  deploy:
    runs-on: arc-runners
    steps:
      - name: Test step
        run: |
          echo "Testing..."